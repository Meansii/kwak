# 접속 비밀번호 (처음 실행할 때 이 값으로 설정됩니다. 이후에는 앱 설정 화면에서 변경하세요)
APP_PASSWORD=원하는_비밀번호

# 서버 포트
PORT=3000

# 로그인 쿠키 서명 키 (비워두면 자동 생성되어 DB에 저장됩니다)
# SESSION_SECRET=아무도_모르는_긴_문자열

# 데이터 저장 폴더 (기본값: ./data)
# DATA_DIR=/var/lib/kwak-data
