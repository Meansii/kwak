# 클라우드에 올려서 인터넷 주소 만들기 (Render 기준)

이 문서대로 하면 `https://...` 주소가 생기고, 매장 컴퓨터를 켜두지 않아도
휴대폰·태블릿·PC 어디서든 접속할 수 있습니다. 처음 한 번만 하면 됩니다. (약 15분)

## 먼저 알아두실 것

- **무료 플랜은 쓰면 안 됩니다.** Render 무료 플랜은 저장 공간이 유지되지 않아
  다시 배포될 때마다 **고객 정보와 사진이 모두 지워집니다.**
  이 앱은 고객 자료를 보관해야 하므로 **디스크가 붙는 유료 플랜(Starter)** 을 씁니다.
- **비용**: Starter 인스턴스 월 $7 + 디스크 1GB 월 $0.25 → **월 약 1만 원** (2026년 8월 기준, 카드 등록 필요)
- 사진을 아주 많이 올리면 디스크를 늘리면 됩니다 (1GB ≈ 축소된 사진 약 3,000~5,000장)

---

## 1단계. Render 가입

1. https://render.com 접속 → 오른쪽 위 **Get Started / Sign Up**
2. **GitHub 계정으로 가입(Sign up with GitHub)** 을 선택하면 저장소 연결이 편합니다.
3. 가입 후 결제 수단 등록: 왼쪽 메뉴 **Billing → Add payment method** 에서 카드 등록

## 2단계. 저장소 연결

1. Render 화면 왼쪽 위 **New +** → **Blueprint** 선택
2. **Connect a repository** 에서 `Meansii/kwak` 를 고릅니다.
   - 목록에 안 보이면 **Configure account** 를 눌러 Render에 이 저장소 접근 권한을 주세요.
3. **Branch** 항목에서 `claude/hair-customer-management-app-8zq57q` 를 선택합니다.
   (나중에 이 브랜치를 `main` 에 합치셨다면 `main` 을 고르면 됩니다.)
4. Render가 저장소 안의 `render.yaml` 을 읽어 설정을 자동으로 채웁니다.

## 3단계. 비밀번호 정하기

배포 화면에 **APP_PASSWORD** 를 입력하는 칸이 나옵니다.

- 매장 직원들이 공용으로 쓸 **접속 비밀번호**를 여기에 적습니다.
- 숫자·영문 섞어서 **8자 이상**으로 정하세요. (예: `hair2026!salon`)
- 다른 항목(NODE_VERSION, DATA_DIR, SESSION_SECRET)은 그대로 두시면 됩니다.

**Apply / Create** 를 누르면 배포가 시작됩니다. 5~10분 정도 걸립니다.

## 4단계. 주소 확인하고 접속

1. 배포가 끝나면 서비스 이름(`kwak-hair-crm`) 위쪽에 주소가 표시됩니다.
   예: `https://kwak-hair-crm.onrender.com`
2. 휴대폰에서 그 주소를 열고, 3단계에서 정한 비밀번호로 로그인합니다.
3. **홈 화면에 추가**하면 앱처럼 쓸 수 있습니다.
   - 아이폰: 사파리 → 공유 버튼 → *홈 화면에 추가*
   - 안드로이드: 크롬 → 오른쪽 위 점 3개 → *홈 화면에 추가*

## 5단계. 처음 들어가서 할 일

1. **설정 → 매장 설정**: 매장 이름 입력
2. **설정 → 비밀번호 변경**: 원하는 비밀번호로 한 번 더 바꿔두면 더 안전합니다
3. **연동 → 구글폼 가져오기**: 지금까지 모인 구글폼 응답 시트를 CSV로 받아 올리기
4. **연동 → 핸즈SOS 내보내기**: 핸즈SOS 업로드 양식에 맞게 열 이름을 맞추고 *양식 저장*

---

## 자동 재배포

이 저장소에 코드가 바뀌면 Render가 알아서 다시 배포합니다.
**고객 자료는 디스크(`/var/data`)에 있어서 재배포해도 그대로 유지됩니다.**

## 백업 (꼭 해두세요)

디스크가 있어도 실수로 서비스를 지우면 자료가 사라집니다. 한 달에 한 번 정도:

- 앱 **설정 → 전체 백업 내려받기** → JSON 파일을 컴퓨터나 구글 드라이브에 보관
- 사진 원본까지 통째로 보관하려면 Render 대시보드에서 디스크 스냅샷 기능을 쓰거나,
  **연동 → 내보내기** 로 엑셀을 받아 두세요.

## 문제가 생겼을 때

| 증상 | 확인할 것 |
|---|---|
| 배포가 실패함 (Build failed) | Render의 **Logs** 탭을 열어 빨간 글씨 확인. `npm ci` 단계 오류면 Node 버전이 22인지 확인 |
| 접속은 되는데 비밀번호가 안 맞음 | Render **Environment** 탭에서 `APP_PASSWORD` 값 확인. 앱에서 한 번 바꾼 뒤에는 **앱 안에서 바꾼 비밀번호**가 우선입니다 |
| 재배포 후 자료가 사라짐 | **Disks** 탭에 `kwak-data` 가 `/var/data` 로 붙어 있는지, `DATA_DIR` 이 `/var/data` 인지 확인 |
| 사진 업로드가 안 됨 | 디스크 용량이 찼을 수 있습니다. **Disks** 에서 사용량 확인 후 크기 늘리기 |

## 다른 서비스를 쓰고 싶다면

Railway, Fly.io 등에서도 동일하게 동작합니다. 조건은 하나뿐입니다.

- **영구 저장 공간(볼륨)을 붙이고**, 환경변수 `DATA_DIR` 을 그 경로로 지정할 것
- 그 밖에 필요한 환경변수: `APP_PASSWORD`(접속 비밀번호), `SESSION_SECRET`(임의의 긴 문자열)
- 실행 명령: `npm ci` 로 설치, `npm start` 로 실행

## 보안 관련

- 인터넷에 공개되는 주소이므로 **비밀번호를 반드시 어렵게** 정하고, 퇴사자가 생기면 바꿔주세요.
- Render는 기본으로 HTTPS(자물쇠) 를 제공하므로 통신 내용은 암호화됩니다.
- 고객 개인정보가 담기는 앱입니다. 주소와 비밀번호를 단체 채팅방 등에 남기지 마세요.
